const config = {
    content: ['./src/**/*.{html,js,svelte,ts}'],

    theme: {
        extend: {
            fontFamily: {
                body: ['Livvic', 'sans-serif'],
            },
        },
    },

    plugins: [],
};

module.exports = config;
